/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/awesome-for-beginners',
    experimental: {
      appDir: true,
    },
}

module.exports = nextConfig
